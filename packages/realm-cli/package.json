{
  "name": "@theaiinc/realm-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "realm": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format esm --clean",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^3.2.0"
  }
}
