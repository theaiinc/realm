{
  "name": "@theaiinc/realm-cli",
  "version": "0.1.0",
  "description": "CLI client for managing Realm execution environments — create, start, stop, exec, and destroy sandboxed realms",
  "keywords": ["realm", "cli", "sandbox", "ai-agent", "execution-environment", "devtools"],
  "type": "module",
  "bin": {
    "realm": "./dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/cli.ts --format esm --clean",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^3.2.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/theaiinc/realm.git",
    "directory": "packages/realm-cli"
  }
}
